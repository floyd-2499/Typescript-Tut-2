let a:string = "Floyd"
const greet:string = "hello"
const time:any = new Date();

console.log(greet, a, "-", time)

// for above if you use ES5 it will compile as "var" for "let and const";
// for if changed to ES6 and above versions "let and const" would be the same ....

//  ! important